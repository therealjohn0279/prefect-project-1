# prefect.yaml
name: hello-world-project

deployments:
  - name: hello-world-deployment
    entrypoint: flows/hello_flow.py:hello_world_flow
    work_pool:
      name: github  # Uses Prefect's managed GitHub execution
    parameters:
      name: "GitHub"